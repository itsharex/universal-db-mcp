# 快速发布指南 - v0.5.3

## ✅ 已完成的修改

### 1. 添加 --auth-source 参数
- 命令行参数：`--auth-source <authSource>`
- 说明：MongoDB 认证数据库（默认为 admin）
- 位置：`src/index.ts`

### 2. 更新版本号
- 从 `0.5.2` 更新到 `0.5.3`

### 3. 重新编译
- 运行 `npm run build` 完成编译

## 🚀 立即发布

```bash
# 1. 发布到 npm
npm publish

# 2. 验证发布
npm view universal-db-mcp version
# 应该显示: 0.5.3
```

## 🧪 用户测试命令

```bash
# 清除 npx 缓存（Windows）
rmdir /s /q "%LOCALAPPDATA%\npm-cache\_npx"

# 测试连接（使用你的 Docker MongoDB）
npx universal-db-mcp@latest \
  --type mongodb \
  --host localhost \
  --port 27017 \
  --user admin \
  --password 123456 \
  --database shop_test \
  --auth-source admin
```

## 📋 Claude Desktop 配置

```json
{
  "mcpServers": {
    "mongodb-db": {
      "command": "npx",
      "args": [
        "universal-db-mcp",
        "--type", "mongodb",
        "--host", "localhost",
        "--port", "27017",
        "--user", "admin",
        "--password", "123456",
        "--database", "shop_test",
        "--auth-source", "admin"
      ]
    }
  }
}
```

## 🎯 预期结果

**成功连接**：
```
🔧 配置信息:
   数据库类型: mongodb
   主机地址: localhost:27017
   数据库名: shop_test
   安全模式: ✅ 只读模式

🔌 正在连接数据库...
✅ 数据库连接成功！
MCP 服务器已启动，等待客户端连接...
```

## 📊 版本历史

| 版本 | 问题 | 解决方案 | 状态 |
|------|------|---------|------|
| 0.5.0 | 模块加载失败 | - | ❌ |
| 0.5.1 | 模块加载失败 | 优化代码 | ❌ |
| 0.5.2 | 模块加载失败 | 降级到 MongoDB 5.9.2 | ✅ |
| 0.5.3 | 认证失败 | 添加 --auth-source 参数 | ✅ |

## ✨ 完美解决

- ✅ 模块加载问题（v0.5.2）
- ✅ 认证问题（v0.5.3）
- ✅ Docker MongoDB 完美支持
- ✅ 所有 MongoDB 认证场景支持

---

**准备发布**: ✅
**版本**: 0.5.3
**日期**: 2026-01-25
